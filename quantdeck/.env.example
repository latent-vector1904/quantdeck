# Optional — only for local private sync (never commit real tokens)
# Create a free DB at: https://console.upstash.com
# Prefer pasting credentials in the app Settings UI instead of baking them into a public deploy.

# VITE_UPSTASH_REST_URL=https://your-db.upstash.io
# VITE_UPSTASH_REST_TOKEN=your-token-here
